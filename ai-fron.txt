ok! here is the deal.

i write a back-end. you write the front-end.

so you just change the "./next" directory.

the current main page of the nextjs is writed by an AI.

the UI is good and doesn't need to be changed so do not change font, colors, modes and other things about theme sctruture.


if you need something in backed just ask me to develop it and then i let you write the front end code.


note: do not write all of your codes in one file

split the file.

let say you want to design a dashboard. 
you'll create "pages/dashboard.tsx"

then init you just import the view from "views/dashboard/index.ts"

each section in the view must be a splitted component in "views/dashboard/components/[section].tsx"

like you want stat component you put it in "/views/dashboard/compenents/stat.tsx"

are you ready?